{
  "version": 3,
  "id": "msrn3ymz-8bon9b",
  "objective": "修复 2026-08-13 全栈代码审查发现的全部 Critical（3 个：SSRF / 凭据外泄 / 存储型 XSS）与全部 Required（后端安全 + 协议解析正确性 + 前端），并复验 go test / npm run build 全绿。Optional/Nit 留作后续，不在本目标范围。",
  "status": "complete",
  "autoContinue": true,
  "usage": {
    "tokensUsed": 577931,
    "activeSeconds": 2205
  },
  "sisyphus": false,
  "createdAt": "2026-08-13T14:55:18.298Z",
  "updatedAt": "2026-08-13T15:35:35.231Z",
  "revision": 90,
  "taskList": {
    "tasks": [
      {
        "id": "fix-config-ssrf",
        "title": "沙箱化配置探测端点出站调用（C1 SSRF + 配套 Required）",
        "status": "complete",
        "verificationContract": "grep -n http.DefaultClient internal/channel/http/config.go 在 handleConfigTest/handleListModels 处无命中；新增单测证明 http://169.254.169.254/ 与 http://127.0.0.1:* 被拒；出站 client 有显式 Timeout；响应体经 io.LimitReader 限流；NewRequestWithContext 错误被处理（畸形 URL 返回 400/502 而非 panic）",
        "completedAt": "2026-08-13T15:02:20.995Z",
        "evidence": "config.go 探测端点改用 probeClient（15s 超时 + Dialer.Control 连接时 IP 二次校验防 DNS rebinding + 重定向再校验）；handleListModels 处理 NewRequestWithContext 错误（畸形 URL 返回 400 非 panic）+ io.LimitReader 10MB 上限。validateProbeEndpo"
      },
      {
        "id": "fix-config-credexfil",
        "title": "堵住 resolveKey 凭据外泄（C2 + pre-existing PUT 合并路径）",
        "status": "complete",
        "verificationContract": "新增单测：name 命中已注册模型但请求 endpoint 与该模型 endpoint 不符时，不发送已存明文 key（请求被拒或 Authorization/x-api-key 不含 key）；PUT /api/config 的空 key 合并逻辑不致 key 指向被篡改的 endpoint",
        "completedAt": "2026-08-13T15:04:41.029Z",
        "evidence": "resolveKey 改为 (string,error) 并加 sameHost 守卫：请求 endpoint 与已保存模型 endpoint 不同 host 时拒绝代填 key；handleConfigPut 合并逻辑同样只在 endpoint 未变时保留旧 key（repoint endpoint + 留空 key → 不代填，防 key 指向被篡改 endpoint）。新增 sameHost"
      },
      {
        "id": "fix-cross-tenant",
        "title": "checkpoint/session 查询按 tenant 作用域（跨租户隔离 Required）",
        "status": "complete",
        "verificationContract": "新增测试：租户 B 携带租户 A 的 session_id 调用 /api/analyze 或 ListCheckpoints，读不到 A 的 checkpoint（空或拒绝）；EnsureSession/AppendCheckpoint/ListCheckpoints 统一按 tenant 作用域",
        "completedAt": "2026-08-13T15:11:16.634Z",
        "evidence": "history 三方法按 tenant 作用域：EnsureSession 跨租户拒绝（session 已存在且 owner 不符 → error，防占用/续传）；ListCheckpoints JOIN sessions 校验归属（他租户读为空）；AppendCheckpoint 写前校验归属。agent.go 把 tenant 透传进 Analyze/AnalyzeStream/Chat/Ch"
      },
      {
        "id": "fix-protocol",
        "title": "修协议解析正确性（R1 openai-response 流式 key + R2 anthropic max_tokens）",
        "status": "complete",
        "verificationContract": "openai-response 流式解析器 tool-call 注册与查找使用一致 key（grep/diff 确认 response.output_item.added 与 response.function_call_arguments.delta 用同一字段）；anthropic buildAnthropic 在 maxTokOf 为 0 时取下限（≥1，如 4096），单测覆盖 max_tokens=0 场景",
        "completedAt": "2026-08-13T15:13:43.897Z",
        "evidence": "R1 修复：parseStreamOpenAIResponse 的 response.output_item.added 改用 item_id 注册（与 arguments.delta 的 item_id 一致），call_id 仅存为 tool call ID；TestParseStreamOpenAIResponseToolArgsKey 用 call_id≠item_id 的 fixture"
      },
      {
        "id": "add-protocol-tests",
        "title": "补协议解析器单元测试（R3）",
        "status": "complete",
        "verificationContract": "internal/llm/protocol_test.go 存在；go test ./internal/llm/ 通过；覆盖 parseOpenAIChat/parseOpenAIResponse/parseAnthropic + 对应流式解析器的 happy-path、tool_calls、body 级 error、空 choices/边界",
        "completedAt": "2026-08-13T15:13:43.899Z",
        "evidence": "新增 internal/llm/protocol_test.go（7 个测试）：3 非流式（parseOpenAIChat 含 happy/tool_calls/body-error/空choices/畸形JSON、parseOpenAIResponse、parseAnthropic 含 stop_reason 推断）+ 3 流式（openai-chat content+tool_call累积、o"
      },
      {
        "id": "fix-frontend",
        "title": "修前端 XSS + Required（C3 + testModel/子文档搜索）",
        "status": "complete",
        "verificationContract": "cd frontend && npx tsc --noEmit && npm run build 全 exit 0；grep rehypeSanitize MemoryDetail.tsx 有命中且在 rehypeRaw 之后；MermaidDiagram.tsx 含 securityLevel:strict；testModel 包在 try/catch 内有错误反馈；MemoryList 产品 tab 在 query 非空时能渲染子文档",
        "completedAt": "2026-08-13T15:16:08.308Z",
        "evidence": "C3 XSS 修复：装 rehype-sanitize@6，MemoryDetail.tsx:163 改 rehypePlugins={[rehypeRaw, rehypeSanitize]}（sanitize 在 raw 之后）；MermaidDiagram.tsx:7 pin securityLevel:\"strict\"。前端 Required：Settings testModel 包 try"
      },
      {
        "id": "verify-all",
        "title": "全套复验 + Critical 清单复核",
        "status": "complete",
        "verificationContract": "go build && go vet && go test ./... 全 exit 0；cd frontend && tsc --noEmit && npm run build 全 exit 0；手动复核 3 个 Critical（SSRF 阻断 / 凭据不外泄 / XSS 已 sanitize）均不再复现",
        "completedAt": "2026-08-13T15:17:21.174Z",
        "evidence": "go build/vet/test ./... 全绿（8 个包 ok，无 FAIL）；frontend tsc --noEmit + npm run build 全 exit 0。3 Critical 复核均闭环：C1 SSRF（探测端点无 DefaultClient、两处 validateProbeEndpoint 守卫、probeClient+Dialer.Control+isForbidde"
      }
    ],
    "blockCompletion": true,
    "proposedAt": "2026-08-13T14:55:12.826Z"
  },
  "stopReason": "agent",
  "archivedPath": ".pi/goals/archived/goal_2026081323353522_msrn3ymz-8bon9b.md"
}

# Goal Prompt

修复 2026-08-13 全栈代码审查发现的全部 Critical（3 个：SSRF / 凭据外泄 / 存储型 XSS）与全部 Required（后端安全 + 协议解析正确性 + 前端），并复验 go test / npm run build 全绿。Optional/Nit 留作后续，不在本目标范围。

## Progress

- Status: complete
- Auto-continue: on
- Sisyphus mode: no
- Time spent: 36m45s
- Tokens used: 578K (577,931) tokens
## Tasks

<!-- blockCompletion: true -->
- [x] fix-config-ssrf: 沙箱化配置探测端点出站调用（C1 SSRF + 配套 Required） — evidence: config.go 探测端点改用 probeClient（15s 超时 + Dialer.Control 连接时 IP 二次校验防 DNS rebinding + 重定向再校验）；handleListModels 处理 NewRequestWithContext 错误（畸形 URL 返回 400 非 panic）+ io.LimitReader 10MB 上限。validateProbeEndpo
- [x] fix-config-credexfil: 堵住 resolveKey 凭据外泄（C2 + pre-existing PUT 合并路径） — evidence: resolveKey 改为 (string,error) 并加 sameHost 守卫：请求 endpoint 与已保存模型 endpoint 不同 host 时拒绝代填 key；handleConfigPut 合并逻辑同样只在 endpoint 未变时保留旧 key（repoint endpoint + 留空 key → 不代填，防 key 指向被篡改 endpoint）。新增 sameHost
- [x] fix-cross-tenant: checkpoint/session 查询按 tenant 作用域（跨租户隔离 Required） — evidence: history 三方法按 tenant 作用域：EnsureSession 跨租户拒绝（session 已存在且 owner 不符 → error，防占用/续传）；ListCheckpoints JOIN sessions 校验归属（他租户读为空）；AppendCheckpoint 写前校验归属。agent.go 把 tenant 透传进 Analyze/AnalyzeStream/Chat/Ch
- [x] fix-protocol: 修协议解析正确性（R1 openai-response 流式 key + R2 anthropic max_tokens） — evidence: R1 修复：parseStreamOpenAIResponse 的 response.output_item.added 改用 item_id 注册（与 arguments.delta 的 item_id 一致），call_id 仅存为 tool call ID；TestParseStreamOpenAIResponseToolArgsKey 用 call_id≠item_id 的 fixture
- [x] add-protocol-tests: 补协议解析器单元测试（R3） — evidence: 新增 internal/llm/protocol_test.go（7 个测试）：3 非流式（parseOpenAIChat 含 happy/tool_calls/body-error/空choices/畸形JSON、parseOpenAIResponse、parseAnthropic 含 stop_reason 推断）+ 3 流式（openai-chat content+tool_call累积、o
- [x] fix-frontend: 修前端 XSS + Required（C3 + testModel/子文档搜索） — evidence: C3 XSS 修复：装 rehype-sanitize@6，MemoryDetail.tsx:163 改 rehypePlugins={[rehypeRaw, rehypeSanitize]}（sanitize 在 raw 之后）；MermaidDiagram.tsx:7 pin securityLevel:"strict"。前端 Required：Settings testModel 包 try
- [x] verify-all: 全套复验 + Critical 清单复核 — evidence: go build/vet/test ./... 全绿（8 个包 ok，无 FAIL）；frontend tsc --noEmit + npm run build 全 exit 0。3 Critical 复核均闭环：C1 SSRF（探测端点无 DefaultClient、两处 validateProbeEndpoint 守卫、probeClient+Dialer.Control+isForbidde

